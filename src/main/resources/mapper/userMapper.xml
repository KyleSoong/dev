<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.song.dev.mapper.UserMapper">	
	<resultMap type="com.song.dev.model.User" id="baseResultMap">
		<id column="id" property="id"/>
		<result column="user_name" property="userName" />
		<result column="password" property="password" />
		<result column="age" property="age" />
		<result column="role" property="role" />
		<result column="flag" property="flag" />
	</resultMap>
	
	<sql id="baseSql">id,user_name,password,age,role,flag</sql>
	
	<select id="getUserById" parameterType="com.song.dev.model.User" resultMap="baseResultMap">
		select <include refid="baseSql" /> from tb_user where id=#{id}
	</select>
</mapper>
